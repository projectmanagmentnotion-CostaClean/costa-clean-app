# CODEX HANDOFF DRAFT — DO NOT EXECUTE

Status: DRAFT ONLY.  
This is not an operational prompt, not a StrategicCommand and not authorization to invoke Codex.

## Purpose

Capture the future implementation intent so a human-approved execution prompt can later be produced without losing the evidence gathered during strategic planning.

## Preconditions before converting this draft into an executable prompt

All must be true:

- strategic package reviewed by human;
- H0 decisions recorded;
- exact implementation branch/base HEAD selected;
- visual authority for Home analytics approved;
- no unresolved M01/M05 cancellation semantics;
- exact allowed dependency changes approved;
- no DB/schema change unless separately authorized;
- current repo status/relevant gates re-audited.

## Future implementation objective

Evolve V3 Home into a period-aware business cockpit that:

- preserves the approved operational priority behavior;
- shows verified financial KPIs;
- provides one primary temporal chart;
- adds only high-signal secondary analytics;
- uses real authenticated Costa Clean data;
- works mobile-first;
- passes existing repository quality/security gates.

## Required evidence to read first

Future agent must re-read current versions of:

- `AGENTS.md`;
- Stitch design constitution/extraction;
- frontend blueprint;
- UX manual;
- Codex workflow;
- app quality gates;
- this phase package;
- relevant current dashboard/closing/payment-state code.

Do not rely on this planning audit if main has materially changed.

## Protected contracts

Future prompt should prohibit unauthorized changes to:

- auth;
- RLS;
- Supabase schema;
- invoice/payment persistence;
- fiscal closing semantics;
- routes;
- production secrets;
- orchestrator control.

## Proposed implementation sequence

1. R1 analytics foundation.
2. R2 KPI/period shell.
3. R3 primary chart and approved chart dependency.
4. R4 invoice state + expense distribution.
5. R5 only approved secondary analytics.
6. R6 responsive/accessibility.
7. R7 measured performance optimization.
8. R8 full regression/release-readiness.

Do not collapse all steps into one unreviewable rewrite if the repository workflow expects bounded gates.

## Metric requirements

Future prompt must explicitly enumerate only metrics approved at H0.

It must say:

- facturado != cobrado;
- no overdue without due date;
- no result/profit unless canonical definition has been approved;
- reuse payment-state/outstanding tolerance rules;
- use actual expense categories;
- no mock production data.

## UI requirements

- existing V3/DS tokens and primitives;
- no second design system;
- exact approved Home visual reference;
- one dominant chart;
- no trading aesthetic;
- no chart-library default palette;
- loading/empty/error/partial states;
- dark/light;
- reduced motion;
- 44px controls.

## Data architecture requirement

Chart/card components receive typed analytics model data and never call Supabase directly.

Initial adapter should use existing authenticated app data unless profiling and the approved sprint justify a narrower reader.

## Dependency intent

Future executable prompt may propose:

- Recharts: likely approved chart engine after compatibility/bundle check.

It should not automatically add:

- Motion for React;
- shadcn;
- Tremor;
- TanStack Table.

Those require demonstrated need and explicit approval in the implementation scope.

## QA intent

Future prompt should require:

- unit metric/date tests;
- integration reconciliation with existing financial helpers;
- component states;
- authenticated E2E;
- responsive matrix;
- accessibility;
- console/network review;
- bundle/performance evidence;
- read-only production smoke only after an independently authorized deployment.

## Stop conditions

Future execution should stop before:

- unexpected schema/RLS/RPC requirement;
- secret/payment requirement;
- destructive or irreversible action;
- unresolved financial semantic conflict;
- missing visual authority when styling would otherwise be invented.

## Explicit current prohibition

Do not execute this draft.

The next gate after strategic planning is human review, not Codex.
