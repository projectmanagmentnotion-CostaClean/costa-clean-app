import { useMemo, useState } from 'react'
import { findExpenseDuplicateGroups } from '../../features/duplicates/duplicateEngine'
import { uploadExpenseReceipt } from '../../features/expenses/expenseAttachmentsApi'
import { createExpense, updateExpense, updateExpenseAttachment } from '../../features/expenses/expenseApi'
import { expenseCategories, expenseDocumentSupportStatuses, expenseDocumentTypes, expenseFiscalReviewStatuses, expenseFiscalRiskLevels, expensePaymentStatuses, type ExpenseListItem, type ExpenseUpsertInput } from '../../features/expenses/types'
import { V3BottomSheet, V3Field, V3Input, V3PrimaryAction, V3SecondaryAction, V3Select, V3Textarea } from '../components/V3Primitives'

interface Props { mode: 'create' | 'edit'; expense?: ExpenseListItem | null; expenses: ExpenseListItem[]; onRefresh: () => Promise<void>; onCompleted: () => void | Promise<void>; onCancel: () => void }
type FormState = { expense_date: string; supplier_name: string; category: string; description: string; document_type: string; payment_status: string; subtotal: string; tax_rate: string; notes: string; document_support_status: string; fiscal_review_status: string; fiscal_risk_level: string; manager_note: string }
const money = (value: string) => { const parsed = Number(value.replace(',', '.')); return Number.isFinite(parsed) ? parsed : Number.NaN }
function initial(expense?: ExpenseListItem | null): FormState { return { expense_date: expense?.expense_date ?? new Date().toISOString().slice(0, 10), supplier_name: expense?.supplier_name ?? '', category: expense?.category ?? 'otros', description: expense?.description ?? '', document_type: expense?.document_type ?? 'ticket', payment_status: expense?.payment_status ?? 'paid', subtotal: String(expense?.subtotal ?? ''), tax_rate: String(expense?.tax_rate ?? 21), notes: expense?.notes ?? '', document_support_status: expense?.document_support_status ?? 'missing', fiscal_review_status: expense?.fiscal_review_status ?? 'pending', fiscal_risk_level: expense?.fiscal_risk_level ?? 'medium', manager_note: expense?.manager_note ?? '' } }
export function V3ExpenseFormFlow({ mode, expense = null, expenses, onRefresh, onCompleted, onCancel }: Props) {
  const [form, setForm] = useState(() => initial(expense)); const [file, setFile] = useState<File | null>(null); const [busy, setBusy] = useState(false); const [error, setError] = useState<string | null>(null)
  const subtotal = useMemo(() => money(form.subtotal), [form.subtotal]); const taxRate = useMemo(() => money(form.tax_rate), [form.tax_rate]); const tax = Number.isFinite(subtotal) && Number.isFinite(taxRate) ? Math.round(subtotal * taxRate) / 100 : Number.NaN; const total = Number.isFinite(subtotal) && Number.isFinite(tax) ? Math.round((subtotal + tax) * 100) / 100 : Number.NaN
  function set<K extends keyof FormState>(key: K, value: FormState[K]) { setForm((current) => ({ ...current, [key]: value })) }
  async function submit(event: React.FormEvent) { event.preventDefault(); setError(null); if (!form.expense_date || !form.supplier_name.trim() || !form.description.trim()) return setError('Fecha, proveedor y descripción son obligatorios.'); if (!Number.isFinite(subtotal) || !Number.isFinite(taxRate)) return setError('Base imponible e IVA deben ser números válidos.'); const candidate = { ...(expense ?? { id: `EXPENSE-DRAFT-${Date.now()}`, expense_date: form.expense_date, supplier_name: form.supplier_name, category: form.category, description: form.description, document_type: form.document_type, payment_status: form.payment_status, subtotal, tax_rate: taxRate, tax_amount: tax, total, is_deductible: true, deductible_percentage: 100, affects_quarterly_closure: true, affects_annual_closure: true, receipt_file_url: null, receipt_file_path: null, attachment_count: 0, document_support_status: form.document_support_status, fiscal_review_status: form.fiscal_review_status, fiscal_risk_level: form.fiscal_risk_level, manager_note: form.manager_note || null, notes: form.notes || null } as ExpenseListItem), expense_date: form.expense_date, supplier_name: form.supplier_name, category: form.category, description: form.description, document_type: form.document_type, payment_status: form.payment_status, subtotal, tax_rate: taxRate, tax_amount: tax, total, document_support_status: form.document_support_status, fiscal_review_status: form.fiscal_review_status, fiscal_risk_level: form.fiscal_risk_level, manager_note: form.manager_note || null, notes: form.notes || null }; const duplicates = findExpenseDuplicateGroups(candidate, expenses).filter((group) => !expense || group.records.some((record) => record.recordId !== expense.id)); if (duplicates.length) return setError('Ya existe un gasto con el mismo contexto. Revisa el gasto existente antes de guardar.'); const payload: ExpenseUpsertInput = { expense_date: form.expense_date, supplier_name: form.supplier_name, category: form.category, description: form.description, document_type: form.document_type, payment_status: form.payment_status, subtotal, tax_rate: taxRate, tax_amount: tax, total, is_deductible: expense?.is_deductible ?? true, deductible_percentage: expense?.deductible_percentage ?? 100, document_support_status: form.document_support_status, fiscal_review_status: form.fiscal_review_status, fiscal_risk_level: form.fiscal_risk_level, manager_note: form.manager_note || null, notes: form.notes || null }; setBusy(true); try { const id = expense ? (await updateExpense(expense.id, payload), expense.id) : await createExpense(payload); if (file) { const uploaded = await uploadExpenseReceipt(id, file); await updateExpenseAttachment(id, uploaded.filePath) } await onRefresh(); await onCompleted() } catch (cause) { setError(cause instanceof Error ? cause.message : 'No se pudo guardar el gasto.') } finally { setBusy(false) } }
  return <V3BottomSheet title={mode === 'create' ? 'Nuevo gasto' : 'Editar gasto'} onClose={onCancel}><form className="v3-form" onSubmit={(event) => void submit(event)}><V3Field label="Fecha"><V3Input type="date" value={form.expense_date} onChange={(event) => set('expense_date', event.target.value)} required /></V3Field><div className="v3-form-grid"><V3Field label="Proveedor"><V3Input value={form.supplier_name} onChange={(event) => set('supplier_name', event.target.value)} required /></V3Field><V3Field label="Categoría"><V3Select value={form.category} onChange={(event) => set('category', event.target.value)}>{expenseCategories.map((value) => <option key={value} value={value}>{value}</option>)}</V3Select></V3Field></div><V3Field label="Descripción"><V3Input value={form.description} onChange={(event) => set('description', event.target.value)} required /></V3Field><div className="v3-form-grid"><V3Field label="Tipo de documento"><V3Select value={form.document_type} onChange={(event) => set('document_type', event.target.value)}>{expenseDocumentTypes.map((value) => <option key={value} value={value}>{value}</option>)}</V3Select></V3Field><V3Field label="Estado de pago"><V3Select value={form.payment_status} onChange={(event) => set('payment_status', event.target.value)}>{expensePaymentStatuses.map((value) => <option key={value} value={value}>{value}</option>)}</V3Select></V3Field></div><div className="v3-form-grid"><V3Field label="Base imponible"><V3Input inputMode="decimal" value={form.subtotal} onChange={(event) => set('subtotal', event.target.value)} required /></V3Field><V3Field label="IVA %"><V3Input inputMode="decimal" value={form.tax_rate} onChange={(event) => set('tax_rate', event.target.value)} required /></V3Field></div><p className="v3-inline-message">Total calculado: {Number.isFinite(total) ? `${total.toFixed(2)} €` : 'pendiente'}</p><div className="v3-form-grid"><V3Field label="Soporte"><V3Select value={form.document_support_status} onChange={(event) => set('document_support_status', event.target.value)}>{expenseDocumentSupportStatuses.map((value) => <option key={value} value={value}>{value}</option>)}</V3Select></V3Field><V3Field label="Revisión"><V3Select value={form.fiscal_review_status} onChange={(event) => set('fiscal_review_status', event.target.value)}>{expenseFiscalReviewStatuses.map((value) => <option key={value} value={value}>{value}</option>)}</V3Select></V3Field></div><V3Field label="Riesgo"><V3Select value={form.fiscal_risk_level} onChange={(event) => set('fiscal_risk_level', event.target.value)}>{expenseFiscalRiskLevels.map((value) => <option key={value} value={value}>{value}</option>)}</V3Select></V3Field><V3Field label="Notas"><V3Textarea value={form.notes} onChange={(event) => set('notes', event.target.value)} /></V3Field><V3Field label="Documento (opcional)"><V3Input type="file" accept="application/pdf,image/jpeg,image/png,image/webp" onChange={(event) => setFile(event.target.files?.[0] ?? null)} /></V3Field>{error ? <p className="v3-inline-message v3-inline-message--error" role="alert">{error}</p> : null}<div className="v3-workspace-actions"><V3SecondaryAction type="button" onClick={onCancel}>Cancelar</V3SecondaryAction><V3PrimaryAction type="submit" disabled={busy}>{busy ? 'Guardando…' : 'Guardar gasto'}</V3PrimaryAction></div></form></V3BottomSheet>
}
