// Public V3 property workspace entrypoint. The implementation lives beside the page
// so list/workspace navigation remains a single V3 surface and the legacy workspace
// is never imported by the V3 renderer.
export { V3PropertyWorkspace } from './V3PropertiesPage'
